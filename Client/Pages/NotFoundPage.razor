@using KitjiStudios.Client.Services
@inject INavigationService NavigationService

<PageTitle>Page Not Found - Kitji Studios</PageTitle>

<div class="min-h-screen bg-background flex items-center justify-center">
    <div class="text-center px-4">
        <div class="mb-8">
            <h1 class="text-9xl font-bold text-primary/20">404</h1>
        </div>
        
        <div class="mb-8">
            <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">Page Not Found</h2>
            <p class="text-xl text-gray-300 max-w-md mx-auto">
                The page you're looking for doesn't exist or has been moved.
            </p>
        </div>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <button @onclick="GoHome" class="btn-primary">
                <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                </svg>
                Go Home
            </button>
            
            <button @onclick="GoBack" class="btn-secondary">
                <svg class="h-4 w-4 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Go Back
            </button>
        </div>
    </div>
</div>

@code {
    private void GoHome()
    {
        NavigationService.NavigateTo("/");
    }

    private void GoBack()
    {
        // In a real application, you might use JavaScript history.back()
        NavigationService.NavigateTo("/");
    }
}

<style>
    .min-h-screen { min-height: 100vh; }
    .bg-background { background-color: hsl(var(--background)); }
    .flex { display: flex; }
    .items-center { align-items: center; }
    .justify-center { justify-content: center; }
    .text-center { text-align: center; }
    .px-4 { padding-left: 1rem; padding-right: 1rem; }
    .mb-8 { margin-bottom: 2rem; }
    .mb-4 { margin-bottom: 1rem; }
    .text-9xl { font-size: 8rem; }
    .text-3xl { font-size: 1.875rem; }
    .text-xl { font-size: 1.25rem; }
    .font-bold { font-weight: 700; }
    .text-white { color: white; }
    .text-gray-300 { color: rgb(209, 213, 219); }
    .max-w-md { max-width: 28rem; }
    .mx-auto { margin-left: auto; margin-right: auto; }
    .flex-col { flex-direction: column; }
    .gap-4 { gap: 1rem; }
    .h-4 { height: 1rem; }
    .w-4 { width: 1rem; }
    .mr-2 { margin-right: 0.5rem; }

    .text-primary\/20 { color: hsl(207 90% 54% / 0.2); }

    @media (min-width: 640px) {
        .sm\\:text-4xl { font-size: 2.25rem; }
        .sm\\:flex-row { flex-direction: row; }
    }
</style>